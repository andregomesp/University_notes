import { SQLite } from '@ionic-native/sqlite';

export function get(id: number) {}
    SQLite.create({
        name: "database.db",
        location: "default"
    })
export function list(params: any) {}